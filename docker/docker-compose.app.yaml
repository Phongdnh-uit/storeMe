services:
  app:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - redis
    # environment:
    volumes:
      - ${STORAGE_ROOT_LOCATION}:/app/storage
    networks:
      - backend
